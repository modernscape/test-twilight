export default function Home() {
  return (
    <main
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        backgroundColor: "#d9d9d9", // 背景を黒にする場合（任意）
        color: "#fff", // 文字を白にする場合（任意）
      }}
    >
      <h1 style={{fontSize: "3rem", fontWeight: "bold"}}>twilight</h1>
    </main>
  )
}
